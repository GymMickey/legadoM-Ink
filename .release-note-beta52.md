## v3.26-beta52 发布说明

### 新功能

- **书架布局弹窗优化**: Activity → `alert()` 对话框模式，消除 E-Ink 下 Activity 过渡闪屏
- **AndroidAlertBuilder 闪屏修复**: `builder.create()` → 样式设置 → `show()`，修复 Flyme 12.6 弹窗闪烁
- **TXT 目录规则 Activity 化**: DialogFragment → TxtTocRuleSelectActivity（照 BookshelfConfigActivity 模式），统一 E-Ink 弹窗行为
- **章节数徽章**: TOC 规则列表每项右侧显示匹配章节数（`tv_chapter_count`），支持渐进式计算

### 技术细节

**书架布局弹窗** (`BaseBookshelfFragment.kt`):
- 删除 `BookshelfConfigActivity` + `activity_bookshelf_config.xml`
- 改为 `alert()` + `DialogBookshelfConfigBinding` 内联弹窗
- 所有保存逻辑与原 Activity 完全一致

**AndroidAlertBuilder** (`AndroidAlertBuilder.kt`):
```kotlin
// 之前：builder.show() 先显示再设样式 → 中间帧闪烁
// 之后：builder.create() → applyTint() → E-Ink 样式 → show()
val dialog = builder.create()
dialog.applyTint()
if (AppConfig.isEInkMode) {
    dialog.window?.run {
        attributes.dimAmount = 0f
        attributes.windowAnimations = 0
        setBackgroundDrawableResource(R.drawable.bg_eink_border_dialog)
    }
}
dialog.show()
```

**TXT 目录规则** (`TxtTocRuleSelectActivity.kt`):
- 新增 Activity，替代原有 DialogFragment
- 8MB 缓冲区分块读取 TXT，单次 IO 并发匹配所有规则正则
- BOM 自动检测 + 首块后立即刷新 UI，消除空白等待感

### 版本信息

| 项目 | 内容 |
|------|------|
| 版本号 | 3.26-beta 52 |
| 提交数 | 25 |
| 发布时间 | 2026-07-22 |

### 下载

- [LegadoM-Ink_正式版_3.26-beta 25_release.apk](https://github.com/GymMickey/legadoM-Ink/releases/download/v3.26-beta52/LegadoM-Ink_正式版_3.26-beta.25_release.apk)

### 备注

E-Ink 弹窗体验持续优化中。如有问题请提交 Issue。