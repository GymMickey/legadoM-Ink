# Legado Max (阅读 Max) — 开发者迁移指南

本文档为接手此项目的开发者提供环境配置说明。

---

## 前置要求

| 软件 | 版本要求 | 备注 |
|------|---------|------|
| JDK | 17+ | 推荐使用 Android Studio 自带 jbr |
| Android Studio | 最新稳定版 | 用于 SDK 管理、模拟器、调试 |
| Android SDK | 至少 api 36 | SDK Platforms + Build-tools 36.1.0 |
| Git | 任意版本 | 非必需，但推荐用于代码管理 |
| Claude Code | 最新版 | 若需使用 AI 辅助开发 |

---

## 第一步：安装 SDK

1. 安装 Android Studio
2. SDK Manager 中安装：
   - SDK Platform 36
   - Android SDK Build-Tools 36.1.0
3. 确认 `local.properties` 中 `sdk.dir` 指向正确的 SDK 路径

---

## 第二步：首次编译

```bash
# 下载 Cronet 依赖（必须）
./gradlew app:downloadCronet

# Debug 编译（验证环境无误）
./gradlew assembleAppMaxDebug

# Release 编译（含 ProGuard 混淆压缩）
./gradlew assembleAppMaxRelease
```

编译成功后 APK 输出路径：
- Debug: `app/build/outputs/apk/appMax/debug/`
- Release: `app/build/outputs/apk/appMax/release/`

---

## 第三步：Claude Code 配置（可选）

若使用 Claude Code 辅助开发：

1. 安装 Claude Code：
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. 在项目根目录启动：
   ```bash
   claude
   ```

3. 项目自带的 `CLAUDE.md` 和 `.claude/skills/` 中的技能会在启动时自动加载。

4. 项目记忆（memory 文件夹）的迁移：
   - 项目根目录下的 `memory/` 文件夹包含所有项目级记忆
   - 在 Claude Code 中可通过读取 `memory/` 目录访问这些内容
   - 若 Claude Code 版本支持项目级记忆（未迁移到用户目录），`memory/` 会自动生效
   - 若不支持，可在新机器上将 `memory/*.md` 内容手动复制到 `~/.claude/projects/<项目名>/memory/` 下

---

## 环境变量（Windows PowerShell）

每次新终端需设置：

```powershell
$env:JAVA_HOME = "D:\Android Studio\jbr"
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
$env:PATH = "$env:JAVA_HOME\bin;$env:ANDROID_HOME\platform-tools;$env:ANDROID_HOME\build-tools\36.1.0;C:\Program Files\Git\bin;$env:PATH"
```

实际路径根据本机 Android Studio 安装位置调整。

---

## 项目关键信息

### 构建

| 命令 | 说明 |
|------|------|
| `./gradlew assembleAppMaxDebug` | Debug 版（appMax 口味） |
| `./gradlew assembleAppMaxRelease` | Release 版（签名+混淆） |
| `./gradlew app:downloadCronet` | 下载 Cronet 网络库（首次必须） |

### 三个产品口味

| 口味 | 包名 | 说明 |
|------|------|------|
| `appMax` | `io.legado.app.yuedu` | 主开发目标，可与原版共存 |
| `appLegacy` | `io.legado.app` | 与原版 Legado 包名一致 |
| `appS` | `io.legado.app.yuedu.a` | 另一个共存版 |

### 前端（Web 设置页）

位于 `modules/web/`，Vue 3 + Vite：

```bash
cd modules/web
pnpm install
pnpm build     # 构建后自动同步到 app 资源目录
```

### 关键注意事项

- 保留原版数据库兼容（Room schema 不改）
- 保留 Backup / Restore / WebDAV 兼容
- `memory/known-bugs.md` 记录了当前已知的设备兼容性问题
- 所有 Phase（1-7）已完成，后续为持续维护阶段

---

## 常见问题

**Q：Release 编译报 R8/ProGuard 错误？**  
A：检查 `app/proguard-rules.pro` 和 `app/cronet-proguard-rules.pro` 是否有缺失规则。不要自行删除或修改混淆规则，优先报告错误。

**Q：Cronet 下载失败？**  
A：需科学上网访问 `storage.googleapis.com`；也可手动下载后放入 `app/cronetlib/`。

**Q：模拟器中文输入法不方便？**  
A：建议在模拟器 Settings → System → Languages & input 中添加中文输入法。

---

*此文件由项目维护者生成，随源码一并交接。*
