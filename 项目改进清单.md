# 项目问题与改进清单

> 记录日期：2026-08-26  
> 范围：Android 主应用、Room 数据层、Rhino 规则引擎、内置 Web 服务与 Vue 管理端。  
> 说明：本文件记录当前需要处理的工程问题，不代表所有项目功能均有缺陷。处理时应优先保证现有书源、阅读数据和旧版本升级兼容性。

## 最高原则：修复过程中的安全行为

> **现有用户的数据安全、覆盖升级能力和核心阅读可用性高于重构速度、代码整洁度及功能增加。**  
> 以下规则是后续人工或 AI 修改代码时必须遵守的硬性约束。无法证明修改安全时，应停止修改、补充验证或请求人工确认，不能依靠猜测继续推进。

### 1. 禁止以丢失用户数据换取修复成功

- 不得使用 `fallbackToDestructiveMigration()`、删除数据库、清空表、卸载重装等方式解决数据库升级问题。
- 不得为了通过 Room Schema 校验而直接修改或删除旧 Schema 文件。
- 不得在没有迁移和兼容方案时修改表名、列名、主键、唯一索引、外键关系或字段语义。
- 不得把“用户可以重新恢复备份”作为允许破坏本地数据的理由；备份本身也可能过期、损坏或不存在。
- 所有批量删除、覆盖写入和恢复操作必须明确目标、支持失败回滚，并在事务或临时文件中完成。

### 2. 必须保持覆盖安装身份不变

- 不得随意修改正式版 `applicationId`、包名、签名证书、Key Alias 或签名体系。
- 不得把 Debug 签名、临时签名或新生成的签名用于正式升级包。
- 不得随意修改数据库文件名、SharedPreferences 文件名、FileProvider authority、Provider authority 和用户文件目录。
- 发布前必须验证旧正式版能够直接覆盖安装新版本，且书架、书源、阅读进度、设置和备份均可正常读取。

### 3. 所有数据格式修改必须向后兼容

- 修改数据库前必须提供从仍在使用的旧版本到当前版本的完整迁移路径。
- 修改备份 JSON、配置键、书源格式或文件目录时，必须继续读取旧格式；写出新格式时应带有明确版本信息。
- 不得直接删除旧 SharedPreferences 键；应先读取并迁移到新键，确认至少经过一个稳定发布周期后再考虑清理。
- 不得改变阅读记录、章节索引、书籍唯一标识和书源唯一标识的含义，除非提供经过测试的数据转换。
- 对未知字段和未来版本字段应尽量宽容处理，避免恢复时因单个字段失败而丢弃整份数据。

### 4. 数据迁移和恢复必须具备原子性

- 数据库迁移必须在事务中执行；失败时保持旧数据可继续打开或明确阻止升级，不能留下半迁移状态。
- 备份恢复应先解析、校验和统计，再开始覆盖现有数据。
- 文件替换应先写入临时文件，校验成功后再原子替换；失败时保留原文件。
- 涉及多张表的数据操作必须明确提交和回滚边界。
- 修复恢复逻辑时，必须测试空备份、损坏备份、旧版备份、部分备份、重复数据和中途失败。

### 5. 不在一次发布中同时进行多项高风险重构

- 数据库迁移、备份恢复重构、`ReadBook` 重构和协程生命周期重构不得集中在同一个正式版本完成。
- 每次修改只解决一个边界清楚的问题，避免顺手改名、格式化无关文件或清理不相关代码。
- 重构阶段尽量保持对外行为、数据结构和文件格式不变；结构调整与功能调整分开提交和验证。
- 核心阅读链路优先采用新旧实现并存、功能开关或小范围启用，验证稳定后再删除旧实现。

### 6. 线程和协程调整不得改变关键写入语义

- 将任务迁移到 `viewModelScope` 或 `lifecycleScope` 前，必须确认页面销毁时是否仍需完成进度、备份或数据库写入。
- 阅读进度、阅读记录、书源保存和备份写入不能因为页面退出或旋转而被意外取消。
- 不得通过捕获并忽略异常来制造“修复成功”；所有数据写入失败必须可记录、可提示或可重试。
- 禁止在未验证调用线程的情况下直接移除 `allowMainThreadQueries()`；应先逐个迁移违规调用，再关闭开关。
- 并发写入同一书籍、章节或阅读记录时，必须有明确的串行化、锁或事务策略。

### 7. 安全加固必须同时考虑现有功能兼容

- 收紧 Provider、Web API、HTTP 或证书策略前，必须列出受影响的第三方调用、旧 Web 页面和旧书源。
- 安全策略不能保持“默认不安全”只为避免兼容问题；应提供受控迁移、显式开关和清晰提示。
- 开启 Web Token 后，Android 内置页面、Vue、WebSocket、备份下载和进度保存必须同步支持认证。
- 不得在日志、Toast、通知、崩溃报告或备份中输出完整 Token、密码、Cookie、Authorization、WebDAV 凭据或签名密码。
- 外部输入，包括 Intent、URI、Provider 参数、书源脚本、备份文件和压缩包，都必须进行大小、路径、类型和权限校验。

### 8. 任何高风险修改必须先有验证手段

- 修改数据库迁移前先补迁移测试；修改备份恢复前先补恢复测试；修改阅读状态前先补进度与会话测试。
- 测试必须使用用户数据的脱敏副本或专用测试数据，禁止直接在唯一一份真实用户数据上试验。
- 至少验证：全新安装、旧版覆盖安装、跨版本升级、升级失败、进程被杀、设备旋转、无网络和存储空间不足。
- 网络测试优先使用 MockWebServer 或本地测试服务，不依赖可能变化的公网接口。
- 测试失败、Lint 严重错误、迁移校验失败或备份恢复不一致时，不得生成正式发布结论。

### 9. 发布必须可追踪、可观察、可停止

- 同一提交应生成可识别且可重复追踪的版本号，不得临时改变版本、渠道或签名。
- 高风险修改先进入测试版或小范围发布，观察崩溃、ANR、升级失败和数据反馈后再进入正式版。
- 发布说明必须明确数据格式变化、认证变化、书源兼容变化及用户是否需要提前备份。
- 数据库版本一旦提高，应考虑旧版应用无法降级打开新数据库；没有必要时不要提高 Room 版本。
- 每项修改应记录影响范围、验证结果和停止发布条件；不能把“回滚 APK”当作数据库回滚方案。

### 10. 文件和仓库操作必须保持克制

- 不得删除、覆盖或回退用户已有的未提交修改和未跟踪文件。
- 不得使用 `git reset --hard`、批量删除目录或其他不可恢复操作处理构建问题。
- 修改前检查 Git 状态；只编辑当前任务需要的文件，并在完成后列出实际变更。
- 生成代码、格式化和依赖升级不得夹带大范围无关改动。
- 密钥、签名文件、Token、`google-services.json`、本地路径和密码不得提交到仓库。

### 11. 高风险变更的最低完成条件

以下条件未全部满足时，高风险修改不得标记为完成：

- [ ] 已说明是否改变数据库、备份、设置、文件目录或外部接口。
- [ ] 已说明现有用户升级路径和失败表现。
- [ ] 已提供自动化测试或可重复的人工验证步骤。
- [ ] 已使用旧版本数据完成覆盖安装验证。
- [ ] 已验证书架、书源、章节、阅读进度、阅读记录和主要设置未丢失。
- [ ] 已验证备份可以生成，并能恢复到测试环境。
- [ ] 已验证签名、应用 ID、版本号和渠道配置正确。
- [ ] 已确认日志和构建产物不包含敏感信息。
- [ ] 已记录已知风险、兼容限制和停止发布条件。

## 优先级与唯一实施顺序

- **P0**：安全边界或数据风险，必须最先处理。
- **Q**：最小质量门禁，为后续功能和重构提供可重复验证。
- **E**：E-Ink 专项，只改变墨水屏主题下的指定界面。
- **M1**：清理朗读、音频与视频残留，直接服务于精简版目标。
- **M2**：统一 Web 服务局域网地址，解决实际使用中的地址漂移和入口不一致。
- **M3**：只处理确认有收益的构建与依赖问题。
- **C**：条件性维护；只有出现可复现故障或测量证据时才实施。

> **下表是本清单唯一有效的总实施顺序。** 后一阶段不得以“顺手处理”为由混入前一阶段的发布；每次只处理一个边界清楚的任务。

| 阶段 | 内容 | 进入下一阶段的条件 |
|---|---|---|
| **第一阶段（P0）** | ReaderProvider、Web Token、网络安全策略 | 外部接口默认安全，现有调用有兼容方案和定向测试 |
| **第二阶段（Q）** | 基础 CI、核心回归测试基线 | Android 与受影响的 Web 检查可重复执行 |
| **第三阶段（E）** | E-P0 静态视觉、E-P1 整页分页、E-P2 分组直切、E-P3 掌阅 PAGE_H 水波纹 | E-Ink 分支通过，普通主题及不支持该私有接口的设备无回归 |
| **第四阶段（M1，原 P3-1）** | 朗读、在线 TTS、音频、视频与歌词残留 | 入口、服务、依赖和权限与精简版功能范围一致 |
| **第五阶段（M2，原 P2-4）** | Web 服务局域网地址统一 | 同一网络连接中的通知、复制地址和二维码一致且可访问 |
| **第六阶段（M3，原 P2-2）** | Glide 去重、依赖漏洞审计和构建边界记录 | 不改变动态 `releaseCode()`，只处理有证据的重复或安全问题 |
| **条件性维护（C）** | 启动阻塞、协程泄漏或后台任务残留 | 只有测量或可复现故障成立时才定点修复 |
| **冻结技术债** | 数据库、阅读核心、备份恢复、超大类、事件总线等全面重构 | 不主动实施；出现真实故障时重新评估最小修复范围 |

## 第一阶段（P0）：安全与外部接口

> **阶段状态：已完成正式包验收。** 2026-09-01 使用正式包 `io.legado.app.yuedu`（`versionName 3.26-beta30`、`versionCode 262442228`）覆盖升级验证通过；Provider 权限、Web Token、NSC、R8 混淆、真实数据兼容均符合本阶段验收范围。已知网络边界记录在 P0-3，不把“验收通过”表述为所有网络风险已经消失。

### P0-1. 限制 ReaderProvider 的外部访问

现状：

- Provider 保留受控外部调用，已使用读写分离的 `signature` 级权限。
- RSS URI、未注册路由、`onCreate()` 返回值和异常输入处理已修正。
- Binder 调用通过可取消的 IO 任务执行；本地操作超时 10 秒，网络操作超时 30 秒。

处理项：

- [x] 确认 ContentProvider 仍需保留受控外部调用。
- [x] 增加 `signature` 级读写权限。
- [x] 修正 RSS URI 与 `RequestCode` 的映射。
- [x] 删除未注册的 `SaveBookProgress` 路由。
- [x] 修正 `onCreate()` 返回值。
- [x] 避免在 Provider 接口中直接使用 `runBlocking` 执行长任务，并增加超时与取消边界。
- [x] 增加权限、URI 路由、异常输入、正常完成、超时取消和 shutdown 取消测试。

完成标准：未授权应用不能读取或修改用户数据；所有已公开 URI 均有测试且行为与 `api.md` 一致。

### P0-2. 完成 Web 服务 Token 认证闭环

现状：

- 新安装默认启用认证；升级用户保留已有认证设置。
- Token 使用 `SecureRandom` 生成，不再使用设备 ID。
- 服务启动后会监听局域网地址，接口包含备份下载、书源删除、书籍修改和阅读进度修改。
- Bearer/Cookie 已覆盖 HTTP、WebSocket、Axios、备份下载和 `sendBeacon`，URL Token 方案已删除。
- CORS 只允许同源或用户明确配置的来源。

处理项：

- [x] 新安装默认启用认证，并生成随机、高熵 Token。
- [x] Vue 前端增加 Token 输入、保存和请求注入机制。
- [x] Axios、WebSocket、备份链接和 `sendBeacon` 使用统一认证方式。
- [x] 保留旧用户已有认证设置，避免升级后 Web 管理端突然不可用。
- [x] 收紧 CORS，只允许同源或用户明确配置的来源。
- [x] 保留删除、恢复、备份等高风险操作的原有确认逻辑。
- [x] 完成认证开启/关闭、错误 Token、Cookie、WebSocket、Token 轮换端到端测试（正式包 10/10 通过）。

已接受边界：

- 当前 Web 服务是局域网 HTTP 服务。Token 能阻止未授权请求，但不能提供传输加密；Bearer 和 Cookie 在不可信局域网中仍可能被嗅探或中间人截获。
- 发布说明和服务启动提示必须明确“仅在可信局域网使用”；不得把 Token 认证描述成 HTTPS 等价保护。

完成标准：认证默认安全可用；开启认证后 Android 内置 Web 页面全部功能正常；无 Token 的局域网请求不能读取或修改数据。

### P0-3. 收紧网络安全配置

现状：

- `network_security_config.xml` 对整个应用允许明文 HTTP。
- 正式配置默认只信任系统 CA，不再信任用户安装的 CA。
- `unsafeSsl` 默认关闭；显式开启后，书源 OkHttp、书源脚本和 WebView 会信任任意证书及主机名，Cronet 同时停用。
- 更新元数据使用 `secureOkHttpClient`，WebDAV 使用独立客户端和独立的 `unsafeWebDav` 开关；书源代理不会进入更新和 WebDAV 客户端。
- 自定义书源可能确实需要 HTTP 或特殊证书，因此不能简单全局禁止后结束处理。

处理项：

- [x] 区分应用自身服务请求与用户书源请求的安全策略。
- [x] 更新元数据及 HTTPS WebDAV 使用系统证书；WebDAV 默认拒绝 HTTP 和 HTTPS→HTTP 降级。
- [x] 将书源 HTTP 和 `unsafeSsl` 限制到用户书源、脚本请求及 WebView；默认关闭。
- [x] 网络日志对 URL、认证头、Cookie 和敏感请求内容进行脱敏。
- [x] 确认代理、WebView、Cronet 和 OkHttp 的策略一致：代理继承书源策略，`unsafeSsl` 开启时禁用 Cronet，官方客户端不继承书源代理。

已知边界与后续项：

- [ ] 将 `unsafeSsl` 的提示改为准确风险说明：开启后是 TrustAll，并同时关闭主机名校验，不只是“兼容用户 CA”或“忽略证书错误”。
- [ ] 对 `unsafeSsl + 代理` 组合给出额外警告；HTTP 代理的 Basic 凭据不具备传输保密性。
- [ ] 分享书源时默认删除代理用户名和密码；备份保留凭据时明确提示备份属于敏感数据。
- [ ] 更新地址虽然先经过 HTTPS 校验，但 APK 实际下载由 Android `DownloadManager` 执行，尚未由 `secureOkHttpClient` 端到端约束 HTTPS→HTTP 重定向；后续决定改为安全专用下载器或明确接受系统下载器边界。
- [ ] `secureOkHttpClient` 当前仍受 `addressCache` 自定义 DNS 影响。TLS 证书和主机名校验仍然有效，但若要求官方链路完全不受用户网络配置影响，应将其隔离。

完成标准：应用自身敏感链路不能被降级到明文或任意证书；兼容性放宽只作用于用户明确授权的书源。

### 第一阶段精简检测

- [x] ReaderProvider 运行权限、URI 路由、异常输入、超时和取消定向测试。
- [x] Web Token 运行认证开启/关闭、错误 Token、Cookie、WebSocket 和 Token 轮换测试；修改 Vue 后追加类型检查与构建。
- [x] 网络策略验证系统 CA、书源 HTTP/证书兼容、WebDAV 降级拒绝和日志脱敏。
- [x] 本阶段未扩展到阅读 UI、E-Ink、完整数据库迁移或无关业务测试。

正式包验收记录：

- ReaderProvider：ADB shell 查询和删除均返回 `SecurityException: Permission Denial`。
- Web Token：10 个场景全部通过，包括 Bearer、Cookie、WebSocket、Token 轮换及关闭认证。
- 网络配置：APK 内 `network_security_config` 与源码一致，默认仅信任系统 CA。
- 覆盖升级：`firstInstallTime` 不变，无崩溃和 Room 迁移错误，176 本真实书籍可读取。
- R8：Provider Manifest 权限、HttpServer 路由、常数时间 Token 比较和 WebSocket 握手均正常。

完成标准：P0 三项均有明确兼容方案、定向测试和停止发布条件，才进入第二阶段。

## 第二阶段（Q）：最小质量门禁

> 本阶段先建立“能稳定发现回归”的最小门禁，不要求在开始 E-Ink 前一次补齐全项目所有测试。P0 接口测试、现有迁移/备份冒烟测试和 Android 基础构建必须先稳定；其余测试随对应阶段逐步补充。

### Q-1. 建立基础持续集成

现状：`.github/workflows/validation.yml` 已纳入版本控制，包含 Android 构建/单测/Lint、Room 迁移测试和 Vue 检查；2026-09-03 的 GitHub Actions Run [33699902287](https://github.com/GymMickey/legadoM-Ink/actions/runs/33699902287) 已在远端稳定通过。

处理项：

- [x] 将 `validation.yml` 纳入版本控制并确认触发条件、权限和缓存配置正确。
- [x] 验证 Android 编译、单元测试和 Lint Job 稳定通过。
- [x] 验证 Room Schema 与迁移测试 Job 在模拟器环境稳定通过。
- [x] 验证 Vue 类型检查、ESLint 和构建 Job 稳定通过；需要前端测试时再增加对应命令。
- [ ] 检查 Web 构建产物与 Android Assets 是否同步。
- [ ] 发布构建使用受保护的签名 Secret，不依赖仓库文件。

完成标准：Android 基础构建和单元测试形成稳定门禁；只有修改 Web 时才要求对应 Vue 检查，失败不能进入发布分支。

### Q-2. 建立核心回归测试基线

最小门禁（量化范围）：

1. 所有任务：执行 `git diff --check`，并运行与改动直接对应的测试集合。
2. Android Kotlin、资源或 Manifest 改动：至少执行 `:app:compileAppMaxDebugKotlin`；涉及打包、资源合并或 R8 时改为执行对应的 `assemble` 任务。
3. Provider、Web 认证或网络安全改动：运行第一阶段已有的 25 项设备定向测试；正式发布时追加签名 Release 覆盖升级和 ADB 未授权访问验证。
4. `modules/web` 改动：执行 `pnpm type-check` 和改动文件的定向 ESLint；涉及产物或依赖时追加 `pnpm build`。
5. 数据库、备份或阅读核心未改动时不运行其专项测试；实际触及对应边界时，至少增加一个可重复的迁移、恢复或阅读会话测试。

优先测试范围：

- [ ] 阅读会话：打开、翻页、切章、切书、恢复进度、异常退出。
- [ ] 书源规则：JS、XPath、JSONPath、JSoup、正则及混合规则。
- [ ] 备份恢复：完整备份、跨版本、部分数据、损坏文件、事务回滚。
- [ ] 数据库迁移：支持的每个升级入口到最新版本。
- [ ] Web API：认证、读取、写入、删除、备份及 CORS。
- [ ] ContentProvider：权限、URI 路由和异常输入。
- [ ] Vue：API 客户端、Token、WebSocket、书源编辑和进度保存。

需要修正的现有测试：

- [x] `UpdateTest.kt` 不应请求 `https://github.com` 首页，应使用稳定且可模拟的发布 API。
- [x] `ExampleInstrumentedTest.kt` 使用正确的 Provider authority 和 URI。
- [x] `MigrationTest.kt` 使用 JUnit 断言替代 Kotlin `assert()`。
- [x] 测试名称和注释从数据库 100 更新到当前版本 102。
- [x] 网络测试使用 MockWebServer，避免依赖公网和第三方服务。

完成标准：核心数据与阅读流程有稳定回归测试；测试不依赖公网；迁移失败能阻止发布。

本阶段远端验收记录：

- 2026-09-03，Run [33699902287](https://github.com/GymMickey/legadoM-Ink/actions/runs/33699902287) 的 `changes`、`android`、`android-quality`、`android-test-compile`、`android-security`、`room-migrations` 和 `web` 七个 Job 均实际运行并通过。
- `android-security` 实际执行 25 项测试，`room-migrations` 实际执行 3 项测试；KVM 检查通过，未出现 `-accel off` 或 `Can't find service: package`。模拟器启动初期出现过短暂的 `adb: device offline` 重试，随后恢复并完成测试。
- 无 Firebase 构建参数、Web type-check/ESLint/build 和 Android Lint baseline 均已生效；baseline 只冻结既有债务，新增 Lint 问题仍会阻断 CI。

### 第二阶段精简检测

- [x] CI 配置自身只验证工作流语法、Android 基础构建和现有单元测试。
- [x] Provider/Web 安全测试承接第一阶段；数据库迁移和备份恢复先保留一个可重复的冒烟入口。
- [x] 不为追求覆盖率一次性扫描或重写全部测试。
- [x] 公网测试改为 MockWebServer 或本地服务后，不再重复请求不稳定站点。

完成标准：后续每个任务都能选择一个定向测试和一个受影响模块构建作为最低门禁。

## 第三阶段（E）：E-Ink 主题专项提升

> **范围已经收口：只实现下列四项；前三项只由用户选择的 E-Ink 主题触发，E-P3 还必须由用户单独主动开启。**
> 普通、暗色等其他主题保持现有颜色、阴影、动画、滚动和手势行为；不得通过品牌、型号、刷新率、Root、系统属性或硬件探测自动启用。

### 专项优先级

| 优先级 | 项目 | 目标 |
|---|---|---|
| **E-P0** | 墨水屏静态视觉规范 | 建立 E-P1/E-P2 共用的黑白、低动效视觉基础 |
| **E-P1** | 搜索和发现结果整页分页 | 减少长列表连续滚动和整屏反复重绘 |
| **E-P2** | 书架分组直接切换 | 消除分组横向拖动和切换动画 |
| **E-P3** | 掌阅 PAGE_H 水波纹翻页（实验性） | 在支持私有接口的掌阅设备上提供可关闭、可回退的硬件翻页波形 |

### E-P0. 墨水屏静态视觉规范

处理项：

- [x] 以现有 `AppConfig.isEInkMode` 为唯一运行时判断；优先复用现有主题属性、颜色函数和 E-Ink drawable，不另存设备状态。
- [x] E-Ink 主题使用纯色、不透明背景；主文字使用不透明黑色，次要文字使用不透明深灰，避免 Alpha 混色。
- [x] 卡片、底栏、弹窗和分组区域取消阴影与 elevation，改用 1dp 实线边框、分隔线和间距表达层级。
- [x] E-Ink 分支关闭 Ripple、渐变、淡入淡出、滑入滑出和仅用于装饰的状态动画；状态变化直接显示最终结果。
- [x] 选中状态使用实心/空心图标、勾选、下划线、字重或边框变化，不能只依赖颜色。
- [x] 禁用状态仍须清晰可辨，不能像参考项目一样把主文字、次要文字、禁用文字和图标全部强制为相同纯黑。
- [x] 不直接全局修改公共 `colors.xml` 破坏普通主题；仅通过 E-Ink 主题属性、专用 drawable 或条件分支应用。
- [x] 检查范围只覆盖本任务实际修改的页面和其共用组件，不进行无边界的全项目视觉重写。

完成标准：E-Ink 主题下目标页面不依赖阴影、透明度或细微颜色变化表达状态；切换到普通主题后视觉和交互与修改前一致。

验收记录（2026-09-03）：Debug 构建通过；设备已确认书架、搜索、发现、阅读记录顶栏及 Toolbar 溢出菜单；普通主题未被强制套用 E-Ink 样式。

### E-P1. 搜索和发现结果整页分页

范围：只改造全局搜索结果和发现详情结果，不扩展到首页、书架列表、书源列表、阅读记录或 RSS。

处理项：

- [x] 抽取可复用的屏幕显示分页逻辑，避免在搜索和发现页面各复制一套状态机。
- [x] 根据列表实际可用高度和已测量条目高度计算每页条数，不硬编码参考项目的约 120dp 行高。
- [x] E-Ink 主题只显示完整一页；提供上一页、当前页码/已加载页数、下一页按钮，换页时整页直接替换并定位顶部。
- [x] 严格区分屏幕显示分页与网络数据分页；到达已加载数据末尾时才沿用现有加载逻辑获取下一批数据。
- [x] 页面重建、返回和字体/窗口尺寸变化时正确恢复或重新计算页码，避免空页、重复项和越界。
- [x] 不增加实体键、音量键、方向键或自定义按键翻页。
- [x] 普通主题继续使用当前连续滚动和自动加载，不显示分页栏，不改变现有数据流。

完成标准：E-Ink 主题下搜索和发现结果可完全通过分页按钮浏览，切页没有平滑滚动；普通主题列表行为不变。

完成记录（2026-09-08）：搜索和发现共用分页器、按可用屏幕高度计算容量、屏幕分页与网络加载边界、返回锚点恢复及普通主题隔离均已完成；分页器定向测试与 AppMax Debug 构建通过。

#### E-P1-R1. 发现分类详情列表容量补充修正

实机问题（2026-09-16）：发现页进入具体分类后，E-Ink 列表虽然按 RecyclerView 可用高度分页，但仍使用默认约 `80×110dp` 封面和较大边距，并按可见条目最大高度估算整页容量；分页稳定后的 `canScrollVertically(1)` 还可能因分页栏 padding 或 header/footer 产生过度减项，导致手机屏幕每页仅显示约 5 本并留下较大空白。

处理项：

- [x] 仅在 E-Ink 主题的发现详情列表模式启用紧凑条目，参考搜索页使用约 `72×96dp` 封面、4dp 边距及受控文字行数；普通主题保持原布局。
- [x] 继续使用 RecyclerView 实际可用高度和实测条目高度计算容量，不按设备型号、分辨率或固定本数硬编码。
- [x] 删除“只要 RecyclerView 仍可向下滚动就整行减一”的粗略判断；改为检查当前页最后一本书的实际 decorated bottom 是否超过扣除分页栏后的可用底边，确实裁切时才减项。
- [x] 列表、网格和瀑布流分别按各自完整条目/完整行计算，不把列表修正扩散到其他布局模式。
- [x] 满页只使用有限的均匀条目间距分配剩余高度，最后一页保持顶部紧凑排列；字体、系统缩放、横竖屏和窗口尺寸变化后重新计算并保留书籍锚点。
- [x] 只运行分页器定向测试、Kotlin 编译和一次 Debug 实机检查；不重复运行安全、Room、Web、Lint 或全量测试。

完成标准：不同屏幕尺寸下每页显示尽可能多的完整书籍条目，页内不滚动、不裁切；允许保留不足一个完整条目高度的自然余量；普通主题、网络分页和发现书源数据流不变。

完成记录（2026-09-16）：发现分类详情页已按实际屏幕可用高度计算完整书目容量，满页剩余高度使用有限间距分布，末页保持顶部排列；Debug 包已在设备上完成实机验收，普通主题、网格、瀑布流、网络分页和书籍数据流未发现回归。

### E-P2. 书架分组直接切换

处理项：

- [x] E-Ink 主题下禁止书架分组 ViewPager 随手指横向拖动，避免两个分组页面持续跟随重绘。
- [x] 点击分组标签或下拉菜单时使用无动画 `setCurrentItem` 直接切换。
- [x] 保留上次选中的分组、标签点击、下拉选择、分组编辑以及书架列表内部的垂直滚动。
- [x] 普通主题继续允许横向拖动和平滑切换，不能因共用 ViewPager 改造而退化。

完成标准：E-Ink 主题下分组只能通过标签或下拉菜单直接切换；普通主题手势和动画保持现状。

完成记录（2026-09-08）：E-Ink 禁止横向拖动；Tab、下拉和恢复分组均无动画直接切换；书架垂直滚动、点击和长按正常；普通主题手势与动画正常；AppMax Debug 构建通过。

### E-P3. 掌阅 PAGE_H 水波纹翻页（实验性）

范围：只为正文无动画翻页增加掌阅 `android.eink.EPDCDevice` 的 PAGE_H 私有接口适配。该能力属于掌阅固件私有接口，不同型号和固件必须以类、方法及实机调用结果为准，不按品牌或型号直接认定支持。

处理项：

- [x] 建立独立的掌阅 PAGE_H 适配器，通过反射缓存 `nativePostCommand(String)` 和 `setForceNextPostMode(int)`；生产代码不得直接链接私有 SDK，也不得扩展到其他 Root/RK/EPD/HAL 接口。
- [x] 增加“掌阅水波纹翻页（实验性）”独立开关，默认关闭；只有 `AppConfig.isEInkMode`、正文无动画翻页、用户主动开启且接口可调用时才生效。接口存在性只能决定功能是否可用，不能自动替用户开启。
- [x] 提供自动/反向两种水波纹方向和慢、中、快三档 PAGE_H 速度，默认自动方向与中速；按 Display rotation 使用文档中的 NEXT/PREV 映射，不复用现有以毫秒表示的触摸或实体键动画速度。
- [x] 仅在 `fillPage(direction)` 确认真正翻页成功后、最终新页面提交前准备 PAGE_H；触摸点击和正文实体键翻页共用同一入口，不能重复发送命令。
- [x] 取消翻页、到达首尾页、首次进入阅读、菜单/弹窗、时间电量更新、样式重排、同页重绘和退出阅读均不得消耗“下一次提交”标记。
- [x] 反射、权限、固件或命令调用失败时立即退化为当前无动画翻页；不得崩溃、卡住翻页或循环重试，错误最多记录一次脱敏诊断信息。
- [x] 非掌阅设备、不支持该类或方法的掌阅固件、普通/暗色主题以及其他软件翻页动画保持原样。

完成标准：至少一台实际支持该 API 的掌阅设备完成上一页、下一页、触摸、正文实体键和旋转方向验证；菜单及同页刷新不会误触发波形；不支持设备无崩溃并保持原无动画翻页。若 Neo2 实测不支持，则保留安全回退并明确标记该机型不可用，不以品牌或型号猜测成功。

完成记录（2026-09-08）：PAGE_H 反射适配、独立开关和三档速度已完成；设置项仅在 E-Ink 主题出现；非无动画模式和不支持接口时不会调用；调用失败可退回现有无动画翻页；定向测试和 AppMax Debug 构建通过；掌阅 Neo2 设备验收通过。

完成记录（2026-09-09）：Debug 包 `io.legado.app.yuedu.debug` 实机验收通过；上一页/下一页的点击、滑动及实体键水波纹正常；自动/反向和三档速度正常；设置入口显示与隐藏正常；“无动画”等选中项在 E-Ink 下保持黑底白字；普通主题未发现异常。

下一阶段：第四阶段 M1“朗读、音频与视频残留清理”。第三阶段 E-P0、E-P1、E-P2、E-P3 已全部完成。后续不再按原 P1→P2→P3 的通用架构重构顺序推进。

### 明确排除

- 除 E-P3 明确限定的掌阅 `android.eink.EPDCDevice` PAGE_H 单次翻页调用外，不实现其他波形切换、立即全刷、翻页计数全刷、Root/RK/EPD/HAL 接口及进入或退出阅读时修改系统刷新模式。
- 不恢复朗读、在线 TTS、音频或视频播放。
- 不实现阅读月报、分享卡片、锁屏壁纸、晃动/倾斜翻页、AI 助手或桌面 Launcher。
- 不增加非正文页面实体键翻页。
- 除 E-P3 在正文无动画翻页成功点插入一次可回退调用外，不重做正文翻页、漫画算法、WebView、首页、阅读记录或全项目长列表。

### 这四项任务的最小验证

- [ ] 修改前后检查 `git status`，只记录和编辑当前任务涉及的文件，不处理用户已有的其他改动。
- [ ] 仅检索受影响类、布局、主题属性及其直接调用方；不默认扫描数据库、Backup/Restore、WebDAV、Provider、Vue 和无关业务模块。
- [ ] 运行 Android 定向编译；如仅改 Android 主模块，优先执行 `compileDebugKotlin`，资源或清单变更再执行对应打包验证。
- [ ] 手工验证目标页面的 E-Ink 主题分支和一个普通主题分支。
- [ ] 只有实际触及数据库、备份格式、网络接口或 Web 前端时，才追加迁移、恢复、接口或 Vue 检查。
- [ ] 验证失败只修复本次变更引入的问题，不顺手重构无关代码。

### 当前执行 Prompt

以下 Prompt 是当前 E-Ink 四项任务的统一执行说明。旧审查记录和旧开发计划不作为这四项任务的默认检查范围。

#### Prompt 0：E-Ink 专项任务默认前缀

这是 Legado M-Ink 项目。当前 E-Ink 专项只包含四项：静态视觉规范、搜索/发现结果整页分页、书架分组直接切换、掌阅 PAGE_H 水波纹翻页。前三项只在 `AppConfig.isEInkMode` 为 `true` 时生效；PAGE_H 还必须由用户单独主动开启并通过接口能力检查，普通和暗色主题保持现状。

硬性边界：

1. 保持数据库、Backup、Restore、WebDAV、书源、RSS、订阅和下载兼容。
2. 除 E-P3 明确限定的掌阅 `android.eink.EPDCDevice` PAGE_H 调用外，不实现其他波形切换、应用内全刷、系统刷新模式接管、Root/RK/EPD/HAL 适配；能力检查不得自动开启功能。
3. 不恢复朗读/TTS、音频、视频、AI、月报、壁纸、晃动翻页、桌面 Launcher 或非正文页面实体键翻页。
4. 优先复用现有 E-Ink 主题、`AppConfig.isEInkMode`、主题属性、drawable、Adapter 和数据流；不重复查询数据库，不为 UI 改造新增 Repository 或统计逻辑。
5. 不全局修改公共颜色或共用控件而破坏普通主题，不处理当前任务之外的代码和用户已有改动。

精简工作流：

1. 检查 Git 状态并定位当前任务的直接文件和调用方，不扫描已明确排除的功能。
2. 先确认现有能力，再做最小范围修改。
3. 只验证当前页面的 E-Ink 主题和一个普通主题分支。
4. 运行 Android 定向编译；只有触及资源或清单时才扩大到打包验证。
5. 数据库、备份、WebDAV、Provider、Vue 和完整安全检查仅在本次确实触及时执行。
6. 修复本次变更引入的错误后停止，不连续修改其他功能。

#### Prompt 1：E-P0 墨水屏静态视觉规范

请只处理本次指定页面及其直接共用组件。E-Ink 主题使用纯色不透明背景、黑色/深灰不透明文字、1dp 实线边框和静态图标状态；取消阴影、elevation、Ripple、渐变及装饰性过渡动画。不得把所有文字和禁用状态统一改成纯黑，不得直接改坏普通主题使用的公共颜色。

完成后验证：E-Ink 主题不依赖阴影、透明度或细微颜色表达状态；普通主题与修改前一致；Android 定向编译通过后停止。

#### Prompt 2：E-P1 搜索和发现结果整页分页

只改全局搜索结果和发现详情结果。E-Ink 主题根据实际可用高度计算每页条数，提供上一页、页码、下一页按钮，换页时整页替换并直接定位顶部；区分屏幕显示分页与现有网络分页。普通主题继续连续滚动和自动加载。

不要扩展到首页、书架列表、书源列表、阅读记录或 RSS；不要增加实体键、音量键、方向键或自定义按键翻页；不要硬编码固定条目高度或复制两套分页状态机。

完成后验证：空数据、单页、多页、继续加载、返回恢复和窗口尺寸变化；普通主题回归；Android 定向编译通过后停止。

#### Prompt 3：E-P2 书架分组直接切换

只改书架分组 ViewPager。E-Ink 主题禁止手指横向拖动，点击标签或下拉菜单时无动画直接切换；保留分组记忆、编辑和书架列表垂直滚动。普通主题继续允许横向拖动和平滑切换。

完成后验证：标签切换、下拉切换、恢复上次分组、普通主题手势；Android 定向编译通过后停止。

#### Prompt 4：E-P3 掌阅 PAGE_H 水波纹翻页

只为 E-Ink 主题下的正文无动画翻页增加实验性 PAGE_H 适配。通过独立反射适配器缓存掌阅类和方法；默认关闭，由用户主动开启；仅在真实翻页成功后、最终新页提交前按实际翻页方向、旋转、自动/反向设置和独立三档速度准备一次 PAGE_H。失败立即静默回退为当前无动画翻页，不得依赖品牌或型号判断，不得影响菜单、同页刷新、其他主题或其他翻页动画。

完成后只运行方向/速度计算单元测试、Android 定向构建，并在支持设备上验证上一页、下一页、触摸、正文实体键、旋转和菜单不误触发；不运行数据库、备份、Web、Provider、全量 Lint 或无关测试。

#### Prompt 5：四项任务最小验收

只检查本次修改涉及的文件和页面：确认 E-Ink 分支生效、普通主题无回归；除 E-P3 已限定的 PAGE_H 单次调用和对现有正文实体键翻页入口的复用外，没有新增硬件刷新或非正文实体键逻辑，也没有改变数据库或业务数据流，并运行 Android 定向编译。除非本次实际触及对应模块，否则不要执行数据库迁移、Backup/Restore、WebDAV、Provider、Vue、全项目依赖或公网接口检测。

发现问题时只修复本次变更引入的问题；列出修改文件、验证命令和未验证项后停止。

## 后续维护清单（收敛版）

> 项目定位为功能基本冻结、单用户、墨水屏优先的阅读器。后续不以通用大型 App 的架构完整性为目标，只处理有直接用户价值、明确安全收益或可复现故障的问题。

> 唯一实施顺序：M1-A → M1-B → M1-C → M2 → M3。C1/C2 只有满足触发条件时才插入；每一项单独修改和验证，不跨任务顺手重构。

## 第四阶段（M1，原 P3-1）：清理朗读、音频与视频残留

现状：产品已经明确不需要朗读、在线 TTS、听书、音频和视频播放，但项目仍可能保留 Media3、ExoPlayer、歌词、HttpTTS、服务、菜单、配置、权限及相关依赖。清理时必须区分生产能力、旧数据兼容和完全死代码。

### M1-A. 只读清点

- [x] 检索朗读、在线 TTS、HttpTTS、音频、视频、Media3、ExoPlayer 和歌词的生产入口及调用链。
- [x] 检查菜单、设置、Manifest、Service、通知、后台任务、Rhino 注册、ProGuard、依赖和资源。
- [x] 将结果分为“生产可达”“旧数据兼容必需”“确认无引用”三类。
- [x] 本步骤只输出证据和删除批次，不修改代码、不删除依赖、不构建 APK。

完成标准：每个待删除项都有直接引用证据或无引用证据；不以名称相似、功能已隐藏或 R8 可能移除作为删除依据。

完成记录（2026-09-10）：已完成生产入口、旧数据兼容、无引用候选和不确定项分类；LyricViewX 已确认只有依赖声明，没有源码、资源、Manifest、反射或 ProGuard 调用；M1-B/M1-C 尚未完成。

### M1-B. 删除明确无引用的播放器与歌词残留

- [x] 按视频、音频、歌词三个功能族分别处理，不在同一批次大范围删除。
- [x] 删除确认没有生产调用方的 Media3、ExoPlayer、播放器、歌词依赖及对应资源、权限和注册项。
- [x] 每批先通过定向编译；全部完成后再统一构建一次 APK，对比体积和方法数。
- [x] 不修改数据库 Schema、书籍类型编号、备份格式或阅读核心。

完成标准：应用中没有音频、视频和歌词播放入口或运行服务；保留的兼容字段不能重新触发播放能力。

完成记录（2026-09-10）：M1-B2B1 已删除无调用方的 Media3/ExoPlayer 专用播放器桥接、Helper、依赖和专用规则；数据库、备份、书籍类型、WebView 视频、外部文件打开及 TTS 均保持不变；M1-B 和 M1 总项仍未完成。

完成记录（2026-09-10）：M1-B2B2B 已定向移除普通网页、底部网页弹窗和 RSS 的视频播放及全屏能力；未修改共享 WebViewPool、书源登录、后台规则、音频、TTS、数据库和备份；M1-B 和 M1 总项仍未完成；其他用户可见 WebView 场景尚未纳入本批次。

完成记录（2026-09-10）：M1-B2B2C 已确认 BookInfoActivity 和 ExploreAdapter 的 WebView 均服务于书源 `<useWeb>`/发现规则，并保留规则注入、登录扩展和来源桥接，未接入视频限制；ReadWebSearchPanel 仅加载用户可见的搜索引擎网页，已接入局部视频请求拦截与页面级视频限制。未修改 WebViewPool、登录、验证码和后台书源链路，未处理音频与 TTS；仍存在规则驱动的用户可见 WebView 视频边界，因此 M1-B 和 M1 总项仍未完成。

完成记录（2026-09-10）：M1-B2B2D 已分别核对三个入口：BookInfoActivity 和 ExploreAdapter 没有可靠的“规则完成→仅展示”边界，仍执行 `<useWeb>` 规则注入、来源桥接或动态页面逻辑，作为书源兼容例外保持不变；ReadWebSearchPanel 不属于书源规则链路，已由 M1-B2B2C 的普通网页视频限制覆盖。本轮未接入 SourceVisibleVideoGuard，未修改书源登录、验证码、后台解析或共享 WebViewPool；不宣称能够阻止规则页面自行解码视频，M1-B 和 M1 总项仍未完成。

完成记录（2026-09-10）：M1-B3A 已区分应用内短音效、正文/RSS TTS、媒体按键阅读控制、WebView 音频、外部音频 Intent 和旧数据兼容；确认不存在独立音频播放器服务。已删除无源码调用的 `androidx.media:media` 直接依赖、专用版本别名及无对应服务的 `FOREGROUND_SERVICE_MEDIA_PLAYBACK` 权限；保留 `MediaButtonReceiver`、TTS、音频焦点配置、`MODIFY_AUDIO_SETTINGS`、WebView/RSS `<audio>`、外部文件打开及音频类型/数据库/备份兼容字段。WebView 音频延后至 M1-B3B，朗读/TTS 延后至 M1-C；M1-B 和 M1 总项仍未完成。

完成记录（2026-09-10）：M1-B3B 已在普通网页、底部网页弹窗、RSS 和网页搜索的可见 WebView 中限制 HTML 音频播放，并保留既有视频限制；BookInfoActivity、ExploreAdapter 继续作为书源兼容例外。未修改 WebViewPool、登录、验证码、后台规则、TTS、音频焦点、媒体按键、数据库或备份；外部下载和 Intent 流程保持原样。M1-B3C（ExplosionView 音效残留）和 M1-C（朗读/TTS）仍未完成，M1-B 与 M1 总项不得勾选。

完成记录（2026-09-10）：M1-B3C 已移除 ExplosionView 中未被调用的 MediaPlayer 音效钩子、声音参数和 `silent_sound.mp3`；ExplosionView 的粒子视觉动画、触发时机、持续时间及结束回调保持不变。当前没有应用主动提供的独立音频/视频/歌词播放器链路；BookInfo/Explore 仍是书源兼容例外，BookType、数据库、备份和外部 Intent 兼容继续保留。TTS/朗读留待 M1-C；M1-B 已完成，但 M1 总项仍未完成。

### M1-C. 清理朗读和 TTS 残留

- [x] 删除朗读服务、在线 TTS、HttpTTS Rhino 注册、相关通知、设置、后台任务和依赖。
- [x] 保留读取旧书籍、旧配置和旧备份所需的最小兼容字段；兼容路径只允许解析或忽略，不恢复朗读能力。
- [x] 不批量删除用户已有数据，不改变数据库 Schema 和备份 JSON 结构。
- [x] 验证正文阅读、切章、进度保存和应用启动未依赖被删除的朗读初始化。

完成标准：生产代码中没有可达的朗读/TTS 服务与入口；旧数据不会导致崩溃，也不会重新启用相关能力。

完成记录（2026-09-16）：M1-C2 已移除 RSS 朗读入口及其正文提取、TTS 调用链，移除正文选择菜单朗读入口，并将点击区域朗读选项从 UI 中移除；旧动作编号保留并安全降级为无操作。系统 TTS 设置未发现用户可见入口，相关 helper、TTS.kt、HttpTTS Entity、数据库和备份兼容继续保留。M1-C 和 M1 总项仍未完成，下一步为 M1-C3：删除实际 TTS/HttpTTS 可执行能力。

完成记录（2026-09-16）：M1-C3 已删除系统 TextToSpeech 运行时、HttpTTS 的 Rhino/AnalyzeUrl 执行桥接、缓存执行入口、专用测试、帮助和管理入口，并移除无调用的朗读权限与通知常量；HttpTTS Entity、DAO、表、Schema、迁移、httpTTS.json 备份校验及旧配置键保持不变。MediaButtonReceiver 及阅读章节控制继续保留，M1-C 和 M1 总项仍未完成，下一步为 M1-C4：统一残留检查与 Debug 验收。

完成记录（2026-09-16）：M1-C4 已按现有流程重建 Web Assets，`httpTTSHelp` 已从 Web 源码和 Android 生成产物移除；TTS 残留检查仅保留 HttpTTS 数据/备份兼容项和历史更新日志。`VisibleWebMediaPolicyTest`、`EInkScreenPaginatorTest`、`IReaderPageHTest` 三组定向测试及 AppMax Debug 构建通过，APK applicationId 为 `io.legado.app.yuedu.debug`；当前无在线设备，启动冒烟和 M1-C、M1 总项仍待实机人工验收。

后续修正（2026-09-16）：实机发现 E-P1 的发现分类详情列表仍存在容量估算过于保守和页底空白问题，已新增 E-P1-R1。应先完成该补充修正并重新构建 Debug 包，再统一进行 M1-C 与 M1 的最终人工验收；在此之前不得勾选 M1-C 或 M1 总项。

最终验收记录（2026-09-16）：E-P1-R1 修正后的 Debug 包已完成实机检查；应用启动、正文阅读、切章、进度保存、书源相关 WebView 兼容路径及本阶段目标功能均正常。M1-C 与第四阶段 M1 验收完成，保留的 HttpTTS 数据、数据库和备份字段仅用于旧数据兼容，不再提供朗读执行能力。

### M1 精简检测

- 每个功能族只运行直接相关的引用检索、Kotlin 编译和一个相邻正文阅读冒烟检查。
- 只有修改 Manifest、资源或依赖后才构建 APK；体积和方法数只在 M1 全部完成时比较一次。
- 不运行数据库迁移、完整备份恢复、Web、Provider、E-Ink 或公网测试。

## 第五阶段（M2，原 P2-4）：统一 Web 服务局域网 IP

现状：`NetworkUtils.getLocalIPAddress()` 会枚举全部非回环 IPv4，`WebService` 和 WiFi 传书入口又分别选择地址。设备同时存在 WiFi、移动网络、VPN、热点或虚拟网卡时，枚举顺序可能变化，导致通知栏、复制地址和二维码显示不同或不可访问的 IP。该问题不涉及 Web Token，也不要求应用固定设备 IP。

处理项：

- [ ] Web 服务和 WiFi 传书共用唯一的地址选择实现，禁止各页面重复枚举网卡。
- [ ] 优先选择当前 WiFi 对应的可用局域网 IPv4；无互联网的局域网仍可传书。
- [ ] 排除回环、链路本地、移动网络、VPN 和无关虚拟网卡；WiFi 不可用时提供确定、可解释的降级结果。
- [ ] 通知栏、设置页、复制地址和二维码使用同一个已选地址。
- [ ] DHCP 确实换址后更新显示；不尝试修改路由器或系统网络配置。
- [ ] 保留端口、Token、HTTP 服务监听和书籍传输行为。

完成标准：同一次网络连接中所有入口显示相同且可访问的 WiFi 地址；开关 Web 服务不会仅因枚举顺序改变地址；开启 VPN 时仍优先提供局域网地址。

### M2 精简检测

- 只运行地址选择单元测试，并人工核对 WiFi、WiFi+VPN 两个场景中的通知、复制地址和二维码。
- 不重复运行数据库、阅读、E-Ink、Room 或完整安全测试。

## 第六阶段（M3，原 P2-2）：构建与依赖定点整理

处理项：

- [ ] 保留动态 `releaseCode()`；记录同一分钟重复构建和系统时间回拨边界，不改成可能导致覆盖安装降级的静态值。
- [ ] 先确认声明和依赖图确实重复，再删除 Glide 核心库重复声明。
- [ ] 运行依赖漏洞和许可证检查，只记录有明确组件、版本和影响路径的结果。
- [ ] 仅升级存在实际安全风险且兼容性可验证的依赖，不批量追新版本。
- [x] Firebase 配置缺失时支持明确的无 Firebase 构建路径。
- [ ] 暂不重构完整版本来源；只有现有 `versionName` 实际造成发布错误时再单独处理。

完成标准：动态 versionCode 覆盖安装策略保持不变；依赖声明无已确认的重复；已知高风险漏洞有明确处置结论。

### M3 精简检测

- Glide 去重只验证依赖图、Kotlin 编译和受影响构建变体。
- 漏洞与许可证检查不自动触发依赖升级；每次升级单独验证。
- 不因构建配置整理运行数据库、阅读、Web 或 E-Ink 专项测试。

## 条件性维护（C）

### C1. 启动阶段网络或 IO 阻塞（原 P1-5）

触发条件：启动测量证明 `App.onCreate()` 中某一网络或 IO 初始化造成可感知延迟、ANR 或稳定性问题。

- [ ] 先测量 `AppWebDav.downloadAllBookProgress()` 等调用是否真的阻塞主线程。
- [ ] 只有证据成立时，延后该单项或改为安全的后台初始化。
- [ ] 不拆分整个 Application，不改变数据库、Rhino、默认数据等核心初始化顺序。

### C2. 协程泄漏或后台任务残留（原 P1-4）

触发条件：存在可复现的页面退出后任务继续、Activity 泄漏、服务停止后任务未取消、异常被吞或关键写入丢失。

- [ ] 只修复有明确调用链的泄漏点或异常路径。
- [ ] 不全局替换自定义 `Coroutine`，不机械清理所有 Scope。
- [ ] 调整生命周期前确认阅读进度、书源保存、备份等关键写入不会被意外取消。

条件性维护不作为常规版本的完成门槛；没有触发证据时保持不实施。

## 冻结技术债：不主动实施

- 全面移除 `allowMainThreadQueries()`，以及按 125 个 UI 文件迁移数据库调用。
- 拆分 `ReadBook`、`ReadBookActivity` 或重建完整阅读状态架构。
- 拆分 `Restore.kt`、`AnalyzeRule.kt`、`AppConfig.kt`、`JsExtensions.kt`。
- 全面替换 LiveEventBus 和全局状态。
- XML 与 Compose 的全项目统一迁移。
- 在不再修改 Vue 前端的前提下重构 Web 资源同步流程。
- 建设完整架构接班文档、清理全项目警告或泛化死代码清理。

冻结项仅作为风险记录保留。出现真实数据问题、崩溃、ANR、泄漏或发布阻塞时，重新建立一个边界明确的最小任务；不得直接恢复原全面重构方案。

## 通用精简检测流程

以下流程适用于六个阶段中的普通子任务：

1. 修改前后执行 `git status --short`，只处理当前任务文件。
2. 使用 `rg` 定位目标类、布局和直接调用方，不扫描已明确无关的模块。
3. 只运行一个与修改直接对应的测试集合；没有现成测试时记录最小人工复现步骤。
4. 只构建受影响模块；Android 资源或 Manifest 改动时执行对应打包，Web 改动时执行 Vue 检查。
5. 手工验证目标流程和一个相邻正常流程。
6. 只有实际跨越数据库、备份、外部接口、Web 或签名边界时，才扩大到对应专项检查。

发现失败时只修复本次变更引入的问题；列出修改文件、验证命令、结果和未验证项后停止。

## 阶段完成记录

- [x] 第一阶段：安全与外部接口（正式包验收完成；P0-3 已知边界保留为后续项）
- [x] 第二阶段：最小质量门禁（2026-09-03，GitHub Actions Run [33699902287](https://github.com/GymMickey/legadoM-Ink/actions/runs/33699902287) 七个 Job 全部实际运行并通过）
- [x] 第三阶段：E-Ink 四项专项（E-P0、E-P1、E-P2、E-P3 均已完成）
- [x] 第四阶段 M1：朗读、音频与视频残留清理（2026-09-16，Debug 实机验收通过）
- [ ] 第五阶段 M2：Web 服务局域网地址统一
- [ ] 第六阶段 M3：Glide 去重与依赖定点审计
- 条件性维护 C1/C2：暂无触发证据时不实施，不作为阶段完成门槛。
- 冻结技术债：数据库、阅读核心、备份恢复、超大类和事件总线等全面重构不进入当前实施计划。

## 正式发布前检查（收敛版）

> 本节只在准备正式发布时执行。所有版本执行“每次必做”；条件项只有本版本实际触及对应边界时才执行。

### 每次发布必做

- [ ] 最终候选提交通过现有 Validation 中实际受影响的 Job；路径规则跳过的无关 Job 不要求人为补跑。
- [ ] 运行本版本修改功能的定向测试，且没有未解释的失败。
- [ ] 目标 AppMax Release 构建成功；只有修改构建配置或渠道逻辑时才扩大到所有构建变体。
- [ ] Release 的应用 ID、`versionName`、动态 `versionCode` 和正式签名正确。
- [ ] 从上一正式版覆盖安装成功，书架、阅读、设置及本版本目标功能可用，数据未被清空。
- [ ] 升级前已提醒用户备份；发布者保留一份可恢复的测试备份。
- [ ] 未提交密钥、Token、`google-services.json`、私有 API 文档或本地配置。
- [ ] 更新说明列出新增功能、兼容限制、已知风险和必要致谢。

### 按修改范围追加

- [ ] 修改 Room Schema、DAO 事务或数据库文件时，运行迁移矩阵及旧数据库升级测试。
- [ ] 修改备份格式或恢复逻辑时，运行完整、旧版、损坏文件和失败回滚测试。
- [ ] 修改 Vue 或 Android Web Assets 时，运行类型检查、Lint、构建和 Assets 一致性校验。
- [ ] 修改 Provider、Web Token、网络安全或 WebDAV 时，运行对应未授权访问和安全定向测试。
- [ ] 删除播放器、TTS、依赖、权限或 Manifest 组件时，核对生产入口不可达，并比较 APK 体积和方法数。
- [ ] 修改普通主题或 E-Ink UI 时，只回归受影响页面及一个相邻主题，不默认扩展到全项目截图检查。
